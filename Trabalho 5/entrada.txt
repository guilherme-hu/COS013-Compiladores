let log = msg => msg asm{println # undefined};

function cria( nome ) {
  let obj = { print: () => log( nome ) };

  return () => obj;
}

let a = cria( "Oi" );

cria( "Nada" )().print();

a().print();
